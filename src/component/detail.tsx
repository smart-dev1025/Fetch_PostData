import { useEffect } from "react";

export function Detail(props: any) {
  useEffect(() => {
    console.log("@@@@@@@@@@@@@@@@@");
  });
  return (
    <div>
      {/* <div>{props.item.userId}</div>
      <div>{props.item.id}</div>
      <div>{props.item.title}</div>
      <div>{props.item.body}</div> */}
    </div>
  );
}
